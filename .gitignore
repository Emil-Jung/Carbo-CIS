# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Secrets & local config
.env
*.env.local

# Build artifacts (desktop app)
build/
dist/
*.spec.bak
desktop/installer/Output/
# Release staging (build on PC, upload via UPLOAD-CIS-TO-SERVER.cmd — never commit):
desktop/cis_download/CarboCIS-Setup.exe
desktop/cis_download/version.json

# Node (if ever used for tooling)
node_modules/

# Logs / OS
*.log
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
