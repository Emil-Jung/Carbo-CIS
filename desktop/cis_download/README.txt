Local staging for CIS releases (NOT committed to git).

After BUILD-INSTALLER.cmd, run:
  powershell -File scripts\stage-cis-download.ps1

Upload CarboCIS-Setup.exe + version.json to the server:
  /opt/carbo/cis/app/  (https://bkweb3.bigk.co.uk/cis/app/)
